package android.telephony;

/**
 * Compile-time stub for the stripped android.jar. Never shipped:
 * the platform boot classboard wins at runtime, so only method
 * names/signatures matter here.
 */
public class ServiceState {
    public int getState() { return 0; }
    public NetworkRegistrationInfo getNetworkRegistrationInfo(int domain, int transportType) { return null; }
}